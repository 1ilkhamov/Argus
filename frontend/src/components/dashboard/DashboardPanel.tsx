// DashboardPanel is no longer used — navigation moved to Sidebar, content renders in main area.
// Kept as placeholder to avoid broken imports during cleanup.
export function DashboardPanel() {
  return null;
}
